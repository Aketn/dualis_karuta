{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "pdf-generator",
      "type": "shell",
      "command": "${command:python.interpreterPath}",
      "args": [
        "generate_pdf.py"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
